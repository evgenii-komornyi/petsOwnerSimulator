import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    settingsContainer: {
        flex: 1,
    },
    systemDetails: {
        flex: 1,
        flexDirection: 'column',
        justifyContent: 'center',
        alignItems: 'center',
        paddingVertical: 10,
    },
    importantWarningContainer: {
        padding: 10,
    },
});
