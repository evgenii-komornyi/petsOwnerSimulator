import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    slotContainer: {
        position: 'absolute',
        zIndex: 9999,
    },
    image: {
        width: '100%',
        height: '100%',
        resizeMode: 'contain',
    },
});
