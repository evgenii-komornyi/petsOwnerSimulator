import { StyleSheet, Dimensions } from 'react-native';

export const styles = StyleSheet.create({
    sofaContainer: {
        position: 'absolute',
        zIndex: 1,
    },
    sofaImage: {
        width: '100%',
        height: '100%',
    },
});
