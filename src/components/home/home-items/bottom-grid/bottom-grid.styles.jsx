import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    bottomGridContainer: {
        flex: 3,
        flexDirection: 'row',
    },
    catHouseContainer: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
    litterContainer: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
});
