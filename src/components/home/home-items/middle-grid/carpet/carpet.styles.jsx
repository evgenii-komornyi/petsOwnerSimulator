import { StyleSheet, Dimensions } from 'react-native';

export const styles = StyleSheet.create({
    carpetContainer: {
        position: 'absolute',
    },
    carpetImage: {
        width: '100%',
        height: '100%',
        flex: 1,
        position: 'relative',
        zIndex: 0,
    },
});
