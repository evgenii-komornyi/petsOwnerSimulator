import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    animationSize: {
        width: 170,
        height: 170,
    },
    lickAnimation: {
        position: 'absolute',
        resizeMode: 'contain',
    },
});
